# Application name
spring.application.name=focusboard

# Thymeleaf
spring.thymeleaf.cache=false

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/focusboard?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=m7S@Ms2!

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server Configuration
server.port=12000
server.address=0.0.0.0

# Mailtrap SMTP Configuration (for testing emails)
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=4cc6c00e0e48e7
spring.mail.password=4df36a633d91e3
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.test-connection=true

# Allow overriding beans
spring.main.allow-bean-definition-overriding=true

# Optional: Logging level (to help debug mail sending and other issues)
logging.level.org.springframework.mail=DEBUG
logging.level.org.hibernate.SQL=DEBUG
